# MongoDB connection string (replace <username>, <password>, <cluster>, and <database-name>)
DB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database-name>?retryWrites=true&w=majority

# Secret key for JWT token (use a secure and random string)
JWT_SECRET=your-secret-key

# Port number for the server
PORT=8000
